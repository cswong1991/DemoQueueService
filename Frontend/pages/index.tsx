import * as React from 'react';
import AdminContainer from '../components/admin/admin-container';

export default function Index() {
  return (
    <AdminContainer>Most functions are restricted in demo</AdminContainer>
  );
}